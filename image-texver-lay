{%- style -%}
.section-{{ section.id }}-padding {
  padding-top: {{ section.settings.padding_top | times: 0.75 | round: 0 }}px;
  padding-bottom: {{ section.settings.padding_bottom | times: 0.75 | round: 0 }}px;
}
@media screen and (min-width: 750px) {
  .section-{{ section.id }}-padding {
    padding-top: {{ section.settings.padding_top }}px;
    padding-bottom: {{ section.settings.padding_bottom }}px;
  }
}
{%- endstyle -%}

<section class="image-text-split color-{{ section.settings.color_scheme }} section-{{ section.id }}-padding" id="image-text-split--{{ section.id }}">
  <div class="image-text-split__inner page-width">

    {% if section.settings.heading != blank or section.settings.subheading != blank %}
      <div class="image-text-split__heading-wrapper">
        {% if section.settings.heading != blank %}
          <h2 class="image-text-split__heading">{{ section.settings.heading }}</h2>
        {% endif %}
        {% if section.settings.subheading != blank %}
          <p class="image-text-split__subheading">{{ section.settings.subheading }}</p>
        {% endif %}
      </div>
    {% endif %}

    <div class="image-text-split__blocks">
      {% for block in section.blocks %}
        <div class="image-text-split__item {% if block.settings.image_position == 'right' %}reverse{% endif %}">
          <div class="image-text-split__image">
            {% if block.settings.image %}
              <img src="{{ block.settings.image | image_url: width: 800 }}" alt="{{ block.settings.title | escape }}">
            {% endif %}
          </div>
          <div class="image-text-split__content color-{{ section.settings.color_scheme2 }} text-{{ block.settings.text_alignment }} align-{{ block.settings.vertical_alignment }}">
            <h2>{{ block.settings.title }}</h2>
            <p>{{ block.settings.text }}</p>
            {%- if block.settings.button_label != blank -%}
              <a
                {% if block.settings.button_link == blank %}
                  role="link" aria-disabled="true"
                {% else %}
                  href="{{ block.settings.button_link }}"
                {% endif %}
                class="button{% if block.settings.button_style_secondary %} button--secondary{% else %} button--primary{% endif %}"
              >
                {{ block.settings.button_label | escape }}
              </a>
            {%- endif -%}
          </div>
        </div>
      {% endfor %}
    </div>

  </div>
</section>

<style>
#image-text-split--{{ section.id }} .image-text-split__blocks {
  display: flex;
  gap: 20px;
}

#image-text-split--{{ section.id }} .image-text-split__content {
  width: 100%;
  display: flex;
  flex-direction: column;
}



#image-text-split--{{ section.id }} .image-text-split__content h2 {
    margin:0px;
}
  

#image-text-split--{{ section.id }} .image-text-split__content {
    padding-top: 18rem;
    padding-bottom: 18rem;
}

  #image-text-split--{{ section.id }} a.button.button--primary {
    /* width: 22rem; */
    margin: auto;
}

#image-text-split--{{ section.id }} .align-top {
  justify-content: flex-start;
}

#image-text-split--{{ section.id }} .align-middle {
  justify-content: center;
}

#image-text-split--{{ section.id }} .align-bottom {
  justify-content: flex-end;
}

#image-text-split--{{ section.id }} .image-text-split__heading-wrapper {
  text-align: center;
  margin-bottom: 60px;
}
  
#image-text-split--{{ section.id }} .image-text-split__subheading {
  max-width: 700px;
  margin: 0 auto;
}
  
#image-text-split--{{ section.id }} .image-text-split__item {
  display: flex;
  flex-direction: column;
  gap: 20px;
  margin-bottom: 60px;
}
  
#image-text-split--{{ section.id }} .image-text-split__item.reverse {
  flex-direction: column-reverse;
}
  
#image-text-split--{{ section.id }} .image-text-split__image img {
  width: 100%;
  display: block;
}
  
#image-text-split--{{ section.id }} .image-text-split__content.text-left {
  text-align: left;
}
  
#image-text-split--{{ section.id }} .image-text-split__content.text-center {
  text-align: center;
}
#image-text-split--{{ section.id }} .image-text-split__content.text-right {
  text-align: right;
}

@media (min-width: 320px) and (max-width: 767px) {
  #image-text-split--{{ section.id }} .image-text-split__blocks {
  display: flex;
  gap: 20px;
  flex-direction: column;
}
  #image-text-split--{{ section.id }} .image-text-split__content {
    padding-top: 8rem;
    padding-bottom: 8rem;
}
  #image-text-split--{{ section.id }} .image-text-split__item {
  margin-bottom: 0px;
}

}
  
@media (min-width: 768px) {
  #image-text-split--{{ section.id }} .image-text-split__item {
    align-items: center;
    justify-content: space-between;
  }
  
  #image-text-split--{{ section.id }} .image-text-split__content,
  #image-text-split--{{ section.id }} .image-text-split__image {
    flex: 1;
  }
}


</style>

{% schema %}
{
  "name": "Image With Text overlay",
  "settings": [
    {
      "type": "text",
      "id": "heading",
      "label": "Heading",
      "default": "Discover the ultimate looks"
    },
    {
      "type": "textarea",
      "id": "subheading",
      "label": "Subheading",
      "default": "We continue to use natural materials to bring you closer to craft."
    },
    {
      "type": "color_scheme",
      "id": "color_scheme",
      "label": "Color Scheme",
      "default": "background-1"
    },
    {
      "type": "color_scheme",
      "id": "color_scheme2",
      "label": "Color Scheme",
      "default": "background-1"
    },
    {
      "type": "range",
      "id": "padding_top",
      "min": 0,
      "max": 100,
      "step": 4,
      "unit": "px",
      "label": "Top Padding",
      "default": 36
    },
    {
      "type": "range",
      "id": "padding_bottom",
      "min": 0,
      "max": 100,
      "step": 4,
      "unit": "px",
      "label": "Bottom Padding",
      "default": 36
    }
  ],
  "blocks": [
    {
      "type": "content_block",
      "name": "Image Text Block",
      "settings": [
        {
          "type": "image_picker",
          "id": "image",
          "label": "Image"
        },
        {
          "type": "text",
          "id": "title",
          "label": "Title",
          "default": "Block Title"
        },
        {
          "type": "textarea",
          "id": "text",
          "label": "Text",
          "default": "Block description goes here."
        },
        {
          "type": "text",
          "id": "button_label",
          "label": "Button Label",
          "default": "Explore"
        },
        {
          "type": "url",
          "id": "button_link",
          "label": "Button Link"
        },
        {
          "type": "checkbox",
          "id": "button_style_secondary",
          "label": "Use Secondary Button Style",
          "default": false
        },
        {
          "type": "select",
          "id": "text_alignment",
          "label": "Text Alignment",
          "default": "left",
          "options": [
            { "value": "left", "label": "Left" },
            { "value": "center", "label": "Center" },
            { "value": "right", "label": "Right" }
          ]
        },
        {
          "type": "select",
          "id": "image_position",
          "label": "Image Position",
          "default": "left",
          "options": [
            { "value": "left", "label": "Left" },
            { "value": "right", "label": "Right" }
          ]
        },
        {
          "type": "select",
          "id": "vertical_alignment",
          "label": "Vertical Alignment",
          "default": "top",
          "options": [
            { "value": "top", "label": "Top" },
            { "value": "middle", "label": "Middle" },
            { "value": "bottom", "label": "Bottom" }
          ]
        }
      ]
    }
  ],
  "presets": [
    {
      "name": "Image Text overlay",
      "category": "Custom",
      "blocks": [
        { "type": "content_block" },
        { "type": "content_block" }
      ]
    }
  ]
}
{% endschema %}
